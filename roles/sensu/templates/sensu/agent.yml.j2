---
name: "{{sensu_agent.name}}"
backend-url:
  - "{{sensu_agent.backend_url}}"
subscriptions:
  "{{sensu_agent.subscriptions}}"
labels:
  entity: "{{sensu_agent.entity}}"
  role: "{{sensu_agent.role}}"
log-level: "{{sensu_agent.log_level}}"
